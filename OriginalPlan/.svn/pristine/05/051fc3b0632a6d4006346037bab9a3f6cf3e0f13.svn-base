$current_enum.enum_name = {
	type = "lib",
	description = "$current_enum.enum_name",
	childs = {
	#set values = $current_enum.values
	#for v in $values
		$v.name = {
			type = "value",
			description = "value=$v.value"
		},
    #end for
	}
},
