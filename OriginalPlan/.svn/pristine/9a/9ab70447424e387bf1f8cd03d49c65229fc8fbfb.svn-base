1. bit_set.h 类型需要添加<<输出函数，以便lua可以将对象转换成string
2. 测试lua可以传回引用及传入引用不?
3. 如果不可以传入引用, 则array需要添加指针类型的函数以供lua调用
4. 将二进制的char类型的函数指针转换成uint8类型的指针, 以免lua中会将char*类型的返回值当成字符串来使用
5. 模板类的重载函数的时候需要将模板类型完全用的明确的类型替换掉, 否则会失败掉
6. 把一些宏替换成函数, 这样lua注册的时候就方便了
7. 定义宏函数的时候顺便定义脚本注册函数, 否则自动注册就做不了
8. 将core中的一些类多添加一些成员获取函数, 这样方便将来扩展
9. lua中创建的每个对象都是指针, 所以需要释放
10. 将引用的地方改成指针参数
11. 添加数据库任务的脚本处理, 可以addUser, newTask等，由脚本将处理函数与task绑定起来
12. 每个类生成自动脚本注册函数, 不能自动生成的通过include来包含
13. 脚本自动注册分成四部分: 类声明注册(自身及基类), 成员函数定义, 成员变量定义, 类中类型定义
14. include处理可以用两种方式: 一种是写成一个头文件(采用同样方式处理), 一种是每个类写成inl文件, 直接在代码中包含s